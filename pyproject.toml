[project]
name = "holy-grail"
version = "0.28.0"
description = "The Holy Grail documentation site"
requires-python = ">=3.11"

[dependency-groups]
dev = ["assertpy>=1.1", "lintro>=0.51.2", "pytest>=9.1.1", "pyyaml>=6.0"]
