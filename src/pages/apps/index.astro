---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import BackToTop from '../../components/BackToTop.astro';
import TableOfContents from '../../components/TableOfContents.astro';
import { withBase } from '../../lib/paths';

const currentPath = withBase('apps/');

interface App {
  name: string;
  href?: string;
  description: string;
  badge?: string;
  badgeType?: 'free' | 'paid' | 'freemium';
  priceBadge?: string;
}

interface Section {
  id: string;
  title: string;
  icon: string;
  note?: string;
  tip?: string;
  tipIcon?: string;
  groups?: { label: string; apps: App[] }[];
  apps?: App[];
}

const sections: Section[] = [
  {
    id: 'password-managers',
    title: 'Password Managers',
    icon: '🔐',
    note: 'Choose one and use it consistently across all your devices.',
    tip: 'Some password managers (like 1Password) have a built-in authenticator, which can replace a separate 2FA app.',
    apps: [
      { name: '1Password', href: 'https://1password.com/', description: 'Built-in authenticator, family/team sharing', badge: 'Paid', badgeType: 'paid', priceBadge: '~$4/mo' },
      { name: 'Bitwarden', href: 'https://bitwarden.com/', description: 'Open source, self-hostable', badge: 'Free / Paid', badgeType: 'free' },
      { name: 'Dashlane', href: 'https://www.dashlane.com/', description: 'VPN included in premium', badge: 'Paid', badgeType: 'paid', priceBadge: '~$5/mo' },
      { name: 'Apple Keychain', description: 'Built into macOS, syncs via iCloud', badge: 'Free', badgeType: 'free' },
    ],
  },
  {
    id: 'authenticators',
    title: 'Authenticator Apps',
    icon: '🛡️',
    apps: [
      { name: 'Ente Auth', href: 'https://ente.io/auth/', description: 'Open source, end-to-end encrypted, all platforms', badge: 'Free', badgeType: 'free' },
      { name: 'Google Authenticator', href: 'https://apps.apple.com/app/google-authenticator/id388497605', description: 'Cloud backup support, mobile', badge: 'Free', badgeType: 'free' },
      { name: 'Microsoft Authenticator', href: 'https://www.microsoft.com/en-us/security/mobile-authenticator-app', description: 'Push notifications for Microsoft accounts, mobile', badge: 'Free', badgeType: 'free' },
    ],
  },
  {
    id: 'tools',
    title: 'Tools & Utilities',
    icon: '🔧',
    groups: [
      {
        label: 'Free',
        apps: [
          { name: 'AltTab', href: 'https://alt-tab-macos.netlify.app/', description: 'Windows-style alt-tab window switcher', badge: 'Free', badgeType: 'free' },
          { name: 'AppCleaner', href: 'https://freemacsoft.net/appcleaner/', description: 'Thoroughly uninstall apps and their leftover files', badge: 'Free', badgeType: 'free' },
          { name: 'Hidden Bar', href: 'https://github.com/dwarvesf/hidden', description: 'Hide menu bar items', badge: 'Free', badgeType: 'free' },
          { name: 'CopyClip', href: 'https://apps.apple.com/app/copyclip-clipboard-history/id595191960', description: 'Clipboard history manager', badge: 'Free', badgeType: 'free' },
          { name: 'Dropover', href: 'https://dropoverapp.com/', description: 'Drag-and-drop shelf for files', badge: 'Free', badgeType: 'free' },
          { name: 'ImageOptim', href: 'https://imageoptim.com/', description: 'Image compression and optimization', badge: 'Free', badgeType: 'free' },
          { name: 'Latest', href: 'https://max.codes/latest/', description: 'Check for app updates', badge: 'Free', badgeType: 'free' },
          { name: 'MonitorControl', href: 'https://github.com/MonitorControl/MonitorControl', description: 'Control external monitor brightness and volume', badge: 'Free', badgeType: 'free' },
          { name: 'Raycast', href: 'https://www.raycast.com/', description: 'Launcher and productivity tool (replaces Spotlight)', badge: 'Free', badgeType: 'free' },
          { name: 'Rectangle', href: 'https://rectangleapp.com/', description: 'Window management with keyboard shortcuts', badge: 'Free', badgeType: 'free' },
          { name: 'Shottr', href: 'https://shottr.cc/', description: 'Screenshot tool with annotations', badge: 'Free', badgeType: 'free' },
          { name: 'TinkerTool', href: 'https://www.bresink.com/osx/TinkerTool.html', description: 'Access hidden macOS settings', badge: 'Free', badgeType: 'free' },
          { name: 'Velja', href: 'https://sindresorhus.com/velja', description: 'Browser picker for opening links', badge: 'Free', badgeType: 'free' },
          { name: 'DevToys', href: 'https://devtoys.app/', description: 'Developer Swiss army knife — JSON, Base64, regex, hash tools', badge: 'Free', badgeType: 'free' },
          { name: 'Ice', href: 'https://github.com/jordanbaird/Ice', description: 'Menu bar management — hide and show items', badge: 'Free', badgeType: 'free' },
          { name: 'Keyclu', href: 'https://github.com/Anze/KeyCluCask', description: 'Shows keyboard shortcuts for the active app', badge: 'Free', badgeType: 'free' },
          { name: 'Find Any File', href: 'https://findanyfile.app/', description: 'Search for files the way Spotlight cannot', badge: 'Paid', badgeType: 'paid', priceBadge: '~$6' },
          { name: 'GrandPerspective', href: 'https://grandperspectiv.sourceforge.net/', description: 'Visualize disk usage with treemaps', badge: 'Free', badgeType: 'free' },
        ],
      },
      {
        label: 'Paid Alternatives',
        apps: [
          { name: 'Alfred', href: 'https://www.alfredapp.com/', description: 'Alternative to Raycast', badge: 'Paid', badgeType: 'paid', priceBadge: '~$35' },
          { name: 'Bartender', href: 'https://www.macbartender.com/', description: 'Alternative to Hidden Bar', badge: 'Paid', badgeType: 'paid', priceBadge: '~$20' },
          { name: 'Magnet', href: 'https://magnet.crowdcafe.com/', description: 'Alternative to Rectangle', badge: 'Paid', badgeType: 'paid', priceBadge: '~$10' },
          { name: 'PopClip', href: 'https://www.popclip.app/', description: 'Text selection actions', badge: 'Paid', badgeType: 'paid', priceBadge: '~$20' },
          { name: 'Maccy', href: 'https://maccy.app/', description: 'Alternative to CopyClip', badge: 'Paid', badgeType: 'paid', priceBadge: '~$10' },
        ],
      },
    ],
  },
  {
    id: 'menu-bar',
    title: 'Menu Bar & Focus',
    icon: '🎛️',
    apps: [
      { name: 'HiddenMe', href: 'https://geni.us/lDuPhC', description: 'Hide desktop icons with a single click', badge: 'Paid', badgeType: 'paid', priceBadge: '~$2' },
      { name: 'Hazeover', href: 'https://hazeover.com/', description: 'Dim background windows to focus on the active one', badge: 'Paid', badgeType: 'paid', priceBadge: '~$12' },
      { name: 'Itsycal', href: 'https://www.mowglii.com/itsycal/', description: 'Tiny menu bar calendar with events', badge: 'Free', badgeType: 'free' },
      { name: 'Lookaway', href: 'https://geni.us/AqAZS7', description: 'Break reminders to reduce eye strain', badge: 'Paid', badgeType: 'paid', priceBadge: '~$15' },
      { name: 'Meetingbar', href: 'https://geni.us/80lGtY0', description: 'Show upcoming meetings in the menu bar', badge: 'Free', badgeType: 'free' },
      { name: 'Hand Mirror', href: 'https://geni.us/GGOUP', description: 'One-click webcam preview in menu bar', badge: 'Freemium', badgeType: 'freemium', priceBadge: '~$5' },
      { name: 'Dato', href: 'https://sindresorhus.com/dato', description: 'Menu bar clock with calendar, time zones, and events', badge: 'Paid', badgeType: 'paid', priceBadge: '~$17' },
      { name: 'Alcove', href: 'https://tryalcove.com/', description: 'Enhance the MacBook notch with useful utilities', badge: 'Paid', badgeType: 'paid', priceBadge: '~$17' },
    ],
  },
  {
    id: 'ai',
    title: 'AI Tools',
    icon: '🤖',
    apps: [
      { name: 'Cursor', href: 'https://cursor.com/', description: 'AI-powered code editor, fork of VS Code', badge: 'Freemium', badgeType: 'freemium', priceBadge: '~$20/mo' },
      { name: 'Upscayl', href: 'https://upscayl.org/', description: 'Open-source AI image upscaler, runs locally', badge: 'Free', badgeType: 'free' },
      { name: 'MacWhisper', href: 'https://goodsnooze.gumroad.com/l/macwhisper', description: 'Audio transcription, runs locally', badge: 'Freemium', badgeType: 'freemium' },
      { name: 'SuperWhisper', href: 'https://superwhisper.com/privacy', description: 'Voice-to-text powered by AI, runs locally', badge: 'Paid', badgeType: 'paid', priceBadge: '~$8/mo' },
    ],
  },
  {
    id: 'communication',
    title: 'Communication',
    icon: '💬',
    apps: [
      { name: 'Slack', href: 'https://slack.com/downloads/mac', description: 'Team communication', badge: 'Freemium', badgeType: 'freemium' },
      { name: 'Discord', href: 'https://discord.com/', description: 'Community and voice chat', badge: 'Freemium', badgeType: 'freemium' },
      { name: 'Telegram', href: 'https://telegram.org/', description: 'Messaging with desktop client', badge: 'Free', badgeType: 'free' },
      { name: 'WhatsApp', href: 'https://www.whatsapp.com/', description: 'Messaging with desktop client', badge: 'Free', badgeType: 'free' },
      { name: 'Zoom', href: 'https://zoom.us/download', description: 'Video conferencing', badge: 'Freemium', badgeType: 'freemium' },
      { name: 'Camo Studio', href: 'https://reincubate.com/camo/', description: 'Use your phone as a high-quality webcam', badge: 'Freemium', badgeType: 'freemium', priceBadge: '~$5/mo' },
    ],
  },
  {
    id: 'ides',
    title: 'IDEs & Editors',
    icon: '✏️',
    apps: [
      { name: 'Visual Studio Code', href: 'https://code.visualstudio.com/', description: 'Extensible, most popular, all languages', badge: 'Free', badgeType: 'free' },
      { name: 'Ghostty', href: 'https://ghostty.org/', description: 'GPU-accelerated terminal emulator', badge: 'Free', badgeType: 'free' },
      { name: 'JetBrains Toolbox', href: 'https://www.jetbrains.com/toolbox-app/', description: 'Manages all JetBrains IDEs', badge: 'Free', badgeType: 'free' },
      { name: 'IntelliJ IDEA', href: 'https://www.jetbrains.com/idea/', description: 'Java, Kotlin', badge: 'Free / Paid', badgeType: 'free' },
      { name: 'PyCharm', href: 'https://www.jetbrains.com/pycharm/', description: 'Python', badge: 'Free / Paid', badgeType: 'free' },
      { name: 'Android Studio', href: 'https://developer.android.com/studio', description: 'Android development, based on IntelliJ', badge: 'Free', badgeType: 'free' },
      { name: 'Xcode', href: 'https://developer.apple.com/xcode/', description: 'Required for iOS/macOS development', badge: 'Free', badgeType: 'free' },
    ],
  },
  {
    id: 'notes-productivity',
    title: 'Notes & Productivity',
    icon: '📝',
    apps: [
      { name: 'Craft', href: 'https://www.craft.do/', description: 'Beautiful native document editor with blocks', badge: 'Freemium', badgeType: 'freemium' },
      { name: 'SideNotes', href: 'https://bit.ly/sidenotes-app', description: 'Quick notes in a slide-out side panel', badge: 'Paid', badgeType: 'paid', priceBadge: '~$20' },
      { name: 'SnippetsLab', href: 'https://apps.apple.com/us/app/snippetslab/id1006087419?mt=12', description: 'Code snippet manager with syntax highlighting', badge: 'Free', badgeType: 'free' },
      { name: 'Pocket', href: 'https://getpocket.com/en/', description: 'Save articles and links to read later', badge: 'Freemium', badgeType: 'freemium' },
      { name: 'Paperless NGX', href: 'https://github.com/paperless-ngx/paperless-ngx?tab=readme-ov-file#getting-started', description: 'Open-source document management system', badge: 'Free', badgeType: 'free' },
      { name: 'Things 3', href: 'https://culturedcode.com/things/', description: 'Elegant task manager with projects, areas, and tags', badge: 'Paid', badgeType: 'paid', priceBadge: '~$50' },
      { name: 'PDFgear', href: 'https://www.pdfgear.com/', description: 'Full-featured PDF editor, reader, and converter', badge: 'Free', badgeType: 'free' },
    ],
  },
  {
    id: 'virtualization',
    title: 'Virtualization',
    icon: '📦',
    apps: [
      { name: 'Docker Desktop', href: 'https://www.docker.com/products/docker-desktop/', description: 'Container management', badge: 'Freemium', badgeType: 'freemium' },
      { name: 'OrbStack', href: 'https://orbstack.dev/', description: 'Fast Docker and Linux VM alternative for macOS', badge: 'Freemium', badgeType: 'freemium' },
    ],
  },
  {
    id: 'workflow',
    title: 'Workflow',
    icon: '⚡',
    apps: [
      { name: 'Figma', href: 'https://www.figma.com/', description: 'Design and prototyping', badge: 'Freemium', badgeType: 'freemium' },
      { name: 'GitHub Desktop', href: 'https://desktop.github.com/', description: 'Git GUI client', badge: 'Free', badgeType: 'free' },
      { name: 'Google Drive', href: 'https://www.google.com/drive/download/', description: 'Cloud storage (includes Docs, Sheets, Slides)', badge: 'Freemium', badgeType: 'freemium' },
      { name: 'iTerm2', href: 'https://iterm2.com/', description: 'Terminal replacement with tabs, split panes, search', badge: 'Free', badgeType: 'free' },
      { name: 'Spaces for Mac', href: 'https://gumroad.com/a/1025595603/grwwi', description: 'Organize apps into workspace groups', badge: 'Paid', badgeType: 'paid', priceBadge: '~$11' },
      { name: 'Portal', href: 'https://portal.app/mac', description: 'Immersive focus and ambient sound app', badge: 'Freemium', badgeType: 'freemium' },
    ],
  },
  {
    id: 'files-media',
    title: 'Files & Media',
    icon: '🎬',
    tip: 'Handbrake and LosslessCut complement each other — use Handbrake for re-encoding and LosslessCut for quick trims without quality loss.',
    tipIcon: '🎞️',
    groups: [
      {
        label: 'Players',
        apps: [
          { name: 'IINA', href: 'https://iina.io/', description: 'Modern media player for macOS', badge: 'Free', badgeType: 'free' },
          { name: 'VLC', href: 'https://www.videolan.org/', description: 'Universal media player', badge: 'Free', badgeType: 'free' },
          { name: 'Spotify', href: 'https://spotify.com/', description: 'Music and podcast streaming', badge: 'Freemium', badgeType: 'freemium', priceBadge: '~$13/mo' },
          { name: 'Stremio', href: 'https://www.stremio.com/', description: 'Media center with streaming aggregation and add-ons', badge: 'Free', badgeType: 'free' },
        ],
      },
      {
        label: 'Editing & Conversion',
        apps: [
          { name: 'Handbrake', href: 'https://handbrake.fr/downloads.php', description: 'Open-source video transcoder', badge: 'Free', badgeType: 'free' },
          { name: 'Lossless Cut', href: 'https://github.com/mifi/lossless-cut', description: 'Quick lossless video/audio trimming and cutting', badge: 'Free', badgeType: 'free' },
          { name: 'Recut', href: 'https://geni.us/PtBZHK', description: 'Automatically remove silences from video/audio', badge: 'Paid', badgeType: 'paid', priceBadge: '~$99' },
          { name: 'Clop', href: 'https://geni.us/tPNILQ', description: 'Clipboard optimizer — auto-compress images and videos', badge: 'Freemium', badgeType: 'freemium' },
          { name: 'HEIC Converter', href: 'https://apps.apple.com/us/app/heic-converter/id1294126402?mt=12', description: 'Convert HEIC images to JPEG or PNG', badge: 'Free', badgeType: 'free' },
        ],
      },
      {
        label: 'Photo Editing',
        apps: [
          { name: 'Lightroom', href: 'https://www.adobe.com/products/photoshop-lightroom.html', description: 'Professional photo editing and management', badge: 'Paid', badgeType: 'paid', priceBadge: '~$12/mo' },
          { name: 'Photomator', href: 'https://www.pixelmator.com/photomator/', description: 'ML-powered photo editor, native macOS', badge: 'Paid', badgeType: 'paid', priceBadge: '~$30/yr' },
        ],
      },
      {
        label: 'File Management',
        apps: [
          { name: 'Forklift', href: 'https://binarynights.com/', description: 'Dual-pane file manager with remote connections', badge: 'Paid', badgeType: 'paid', priceBadge: '~$20' },
          { name: 'Transnomino', href: 'https://www.transnomino.com/', description: 'Powerful batch file renaming tool', badge: 'Free', badgeType: 'free' },
        ],
      },
    ],
  },
  {
    id: 'screenshots',
    title: 'Screenshots & Recording',
    icon: '📸',
    apps: [
      { name: 'XNIP', href: 'https://xnipapp.com/', description: 'Scrolling screenshot capture with annotations', badge: 'Freemium', badgeType: 'freemium' },
      { name: 'Zappy', href: 'https://zapier.com/zappy', description: 'Quick screen capture and GIF recording by Zapier', badge: 'Free', badgeType: 'free' },
    ],
  },
  {
    id: 'clipboard-input',
    title: 'Clipboard & Input',
    icon: '📋',
    apps: [
      { name: 'PastePal', href: 'https://bit.ly/47hcD10', description: 'Clipboard manager with visual preview and organization', badge: 'Paid', badgeType: 'paid', priceBadge: '~$9' },
      { name: 'Rocket', href: 'https://bit.ly/4cW9Ifl', description: 'Slack-style emoji picker for any app', badge: 'Freemium', badgeType: 'freemium' },
    ],
  },
  {
    id: 'security-privacy',
    title: 'Security & Privacy',
    icon: '🔒',
    apps: [
      { name: 'Encrypto', href: 'https://macpaw.audw.net/BXomB1', description: 'Encrypt files before sending with AES-256', badge: 'Free', badgeType: 'free' },
      { name: 'NordVPN', href: 'https://nordvpn.com/', description: 'VPN for privacy, security, and geo-unblocking', badge: 'Paid', badgeType: 'paid', priceBadge: '~$3/mo' },
      { name: 'Clyde', href: 'https://theclyde.app/', description: 'Privacy-focused Mac cleaner and optimizer', badge: 'Paid', badgeType: 'paid' },
    ],
  },
  {
    id: 'system-maintenance',
    title: 'System & Maintenance',
    icon: '⚙️',
    apps: [
      { name: 'Al Dente', href: 'https://apphousekitchen.com/', description: 'Battery charge limiter to prolong battery health', badge: 'Freemium', badgeType: 'freemium' },
      { name: 'Batteries', href: 'https://www.fadel.io/batteries', description: 'Track battery levels of all your Apple devices', badge: 'Paid', badgeType: 'paid', priceBadge: '~$9' },
      { name: 'BetterDisplay', href: 'https://github.com/waydabber/BetterDisplay', description: 'Custom resolutions, XDR brightness, display management', badge: 'Freemium', badgeType: 'freemium', priceBadge: '~$22' },
      { name: 'CleanMyMac X', href: 'https://bit.ly/MacApps2023', description: 'System cleanup, malware removal, and optimization', badge: 'Paid', badgeType: 'paid', priceBadge: '~$10/mo' },
      { name: 'Disk Drill', href: 'https://www.cleverfiles.com/', description: 'Data recovery and disk health monitoring', badge: 'Freemium', badgeType: 'freemium', priceBadge: '~$89' },
      { name: 'Display Menu', href: 'https://apps.apple.com/us/app/display-menu/id549083868', description: 'Quickly change display resolution from the menu bar', badge: 'Freemium', badgeType: 'freemium', priceBadge: '~$2' },
      { name: 'DisplayLink Manager', href: 'https://www.synaptics.com/products/displaylink-graphics/downloads/macos', description: 'Driver for DisplayLink docking stations and adapters', badge: 'Free', badgeType: 'free' },
      { name: 'Raze', href: 'https://bit.ly/3XgzzIX', description: 'Monitor and manage system resources', badge: 'Paid', badgeType: 'paid' },
      { name: 'DumbWare', href: 'https://github.com/DumbWareio', description: 'Collection of simple, open-source self-hosted tools', badge: 'Free', badgeType: 'free' },
    ],
  },
  {
    id: 'web-email',
    title: 'Web & Email',
    icon: '📧',
    apps: [
      { name: 'TempBox', href: 'https://geni.us/BA6e7Wt', description: 'Disposable temporary email addresses', badge: 'Free', badgeType: 'free' },
      { name: 'Sparkle', href: 'https://bit.ly/3B6k3rF', description: 'Smart email client with AI triage', badge: 'Freemium', badgeType: 'freemium' },
    ],
  },
  {
    id: 'design-creative',
    title: 'Design & Creative',
    icon: '🎨',
    apps: [
      { name: 'Barrel', href: 'https://getbarrel.app/pricing/', description: '3D app icon and UI mockup generator', badge: 'Paid', badgeType: 'paid' },
      { name: 'Wallpapers', href: 'https://andrewethanzeng.gumroad.com/', description: 'Curated high-resolution desktop wallpapers', badge: 'Paid', badgeType: 'paid' },

    ],
  },
  {
    id: 'remote-desktop',
    title: 'Remote Desktop',
    icon: '🖥️',
    apps: [
      { name: 'AnyDesk', href: 'https://anydesk.com/', description: 'Fast remote desktop with low latency', badge: 'Freemium', badgeType: 'freemium', priceBadge: '~$13/mo' },
      { name: 'RustDesk', href: 'https://rustdesk.com/', description: 'Open-source remote desktop, self-hostable', badge: 'Free', badgeType: 'free' },
    ],
  },
  {
    id: 'gaming-streaming',
    title: 'Gaming & Streaming',
    icon: '🎮',
    apps: [
      { name: 'Steam Link', href: 'https://apps.apple.com/us/app/steam-link/id1246969117', description: 'Stream games from your PC to your Mac', badge: 'Free', badgeType: 'free' },
      { name: 'MetaTrader 5', href: 'https://www.metatrader5.com/en/download', description: 'Trading platform for forex and stocks', badge: 'Free', badgeType: 'free' },
    ],
  },
  {
    id: 'smart-home',
    title: 'Smart Home',
    icon: '🏠',
    apps: [
      { name: 'Home Assistant', href: 'https://companion.home-assistant.io/', description: 'Open-source home automation companion app', badge: 'Free', badgeType: 'free' },
      { name: 'NetSpot', href: 'https://www.netspotapp.com/', description: 'WiFi survey, analysis, and troubleshooting', badge: 'Freemium', badgeType: 'freemium', priceBadge: '~$149' },
    ],
  },
  {
    id: 'browsers',
    title: 'Browsers',
    icon: '🌐',
    groups: [
      {
        label: 'Recommended',
        apps: [
          { name: 'Arc', href: 'https://arc.net/', description: 'Chromium-based, tab management, spaces, built-in tools', badge: 'Free', badgeType: 'free' },
          { name: 'Google Chrome', href: 'https://www.google.com/chrome/', description: 'Most widely used, strong DevTools', badge: 'Free', badgeType: 'free' },
          { name: 'Firefox', href: 'https://www.mozilla.org/firefox/', description: 'Privacy-focused, independent engine', badge: 'Free', badgeType: 'free' },
          { name: 'Safari', href: 'https://www.apple.com/safari/', description: 'Built into macOS, battery efficient', badge: 'Free', badgeType: 'free' },
          { name: 'Zen', href: 'https://zen-browser.app/', description: 'Privacy-focused browser built on Firefox', badge: 'Free', badgeType: 'free' },
        ],
      },
      {
        label: 'Extensions',
        apps: [
          { name: 'Dark Reader', description: 'Dark mode for all websites (Chrome, Firefox, Safari)', badge: 'Free', badgeType: 'free' },
          { name: 'uBlock Origin', description: 'Ad and tracker blocker (Chrome, Firefox)', badge: 'Free', badgeType: 'free' },
          { name: '1Password / Bitwarden', description: 'Password manager integration (Chrome, Firefox, Safari)', badge: 'Free', badgeType: 'free' },
        ],
      },
    ],
  },
];

// Build headings for TableOfContents
const tocHeadings = [
  ...sections.map(s => ({ depth: 2, slug: s.id, text: s.title })),
  { depth: 2, slug: 'good-to-know', text: 'Good to Know' },
];

const totalApps = sections.reduce((acc, s) => {
  if (s.groups) return acc + s.groups.reduce((g, gr) => g + gr.apps.length, 0);
  return acc + (s.apps?.length || 0);
}, 0);

function getDomain(href?: string): string {
  if (!href) return '';
  try { return new URL(href).hostname.replace('www.', ''); }
  catch { return ''; }
}

function faviconUrl(href?: string): string {
  const domain = getDomain(href);
  return domain ? `https://www.google.com/s2/favicons?domain=${domain}&sz=32` : '';
}
---

<BaseLayout title="Recommended Apps" description="A curated list of recommended macOS applications for developers">
  <Header slot="header" currentPath={currentPath} />

  <section class="apps-page" data-pagefind-body>
    <div class="container">

      <!-- ── Hero ──────────────────────────────────── -->
      <header class="hero">
        <div class="hero-ornament" aria-hidden="true">
          <span class="orn-line"></span>
          <span class="orn-diamond">◆</span>
          <span class="orn-line"></span>
        </div>
        <h1>Recommended Apps</h1>
        <p class="hero-sub">{totalApps} hand-picked macOS applications for developers &amp; power&nbsp;users</p>

        <!-- Search + Filters -->
        <div class="controls">
          <div class="search-box">
            <svg class="search-svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
              <circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/>
            </svg>
            <input type="text" id="app-search" placeholder="Search apps..." autocomplete="off" />
          </div>
          <div class="price-filters" role="group" aria-label="Filter by price">
            <button class="pf active" data-filter="all">All</button>
            <button class="pf" data-filter="free"><span class="pf-dot free-dot"></span>Free</button>
            <button class="pf" data-filter="freemium"><span class="pf-dot freemium-dot"></span>Freemium</button>
            <button class="pf" data-filter="paid"><span class="pf-dot paid-dot"></span>Paid</button>
          </div>
        </div>
      </header>

      <!-- ── Two-column layout ─────────────────────── -->
      <div class="apps-layout">
        <div class="apps-main">

          <!-- Mobile TOC (hidden on desktop via component CSS) -->
          <div class="apps-toc-mobile">
            <TableOfContents headings={tocHeadings} />
          </div>

          <!-- No Results -->
          <div class="no-results" id="no-results" style="display:none;">
            <p>No apps match your search.</p>
          </div>

          <!-- Sections -->
        {sections.map((section) => (
          <section id={section.id} class="sect" data-sect>
            <div class="sect-head">
              <span class="sect-icon">{section.icon}</span>
              <div class="sect-titles">
                <h2>{section.title}</h2>
                {section.note && <p class="sect-note">{section.note}</p>}
              </div>
            </div>

            {section.groups ? (
              section.groups.map((group) => (
                <div class="grp" data-grp>
                  <h3 class="grp-label">{group.label}</h3>
                  <div class="cards">
                    {group.apps.map((app) => (
                      <article class="card" data-name={app.name.toLowerCase()} data-desc={app.description.toLowerCase()} data-price={app.badgeType || 'none'}>
                        {app.badge && <span class={`badge badge-type ${app.badgeType || 'free'}`}>{app.badge}</span>}
                        <div class="card-header">
                          <div class="card-fav-wrap">
                            {faviconUrl(app.href) ? (
                              <>
                                <img class="card-fav" src={faviconUrl(app.href)} alt="" width="20" height="20" loading="lazy" onerror="this.style.display='none';this.nextElementSibling.style.display='flex';" />
                                <span class="card-fav-fb" aria-hidden="true">{section.icon}</span>
                              </>
                            ) : (
                              <span class="card-fav-fb" style="display:flex" aria-hidden="true">{section.icon}</span>
                            )}
                          </div>
                          <div class="card-meta">
                            {app.href ? (
                              <a href={app.href} class="card-name" target="_blank" rel="noopener noreferrer">{app.name}<span class="ext" aria-hidden="true">&thinsp;↗</span></a>
                            ) : (
                              <span class="card-name">{app.name}</span>
                            )}
                            {app.priceBadge && <span class="badge price">{app.priceBadge}</span>}
                          </div>
                        </div>
                        <p class="card-desc">{app.description}</p>
                      </article>
                    ))}
                  </div>
                </div>
              ))
            ) : section.apps ? (
              <div class="cards">
                {section.apps.map((app) => (
                  <article class="card" data-name={app.name.toLowerCase()} data-desc={app.description.toLowerCase()} data-price={app.badgeType || 'none'}>
                    {app.badge && <span class={`badge badge-type ${app.badgeType || 'free'}`}>{app.badge}</span>}
                    <div class="card-header">
                      <div class="card-fav-wrap">
                        {faviconUrl(app.href) ? (
                          <>
                            <img class="card-fav" src={faviconUrl(app.href)} alt="" width="20" height="20" loading="lazy" onerror="this.style.display='none';this.nextElementSibling.style.display='flex';" />
                            <span class="card-fav-fb" aria-hidden="true">{section.icon}</span>
                          </>
                        ) : (
                          <span class="card-fav-fb" style="display:flex" aria-hidden="true">{section.icon}</span>
                        )}
                      </div>
                      <div class="card-meta">
                        {app.href ? (
                          <a href={app.href} class="card-name" target="_blank" rel="noopener noreferrer">{app.name}<span class="ext" aria-hidden="true">&thinsp;↗</span></a>
                        ) : (
                          <span class="card-name">{app.name}</span>
                        )}
                        {app.priceBadge && <span class="badge price">{app.priceBadge}</span>}
                      </div>
                    </div>
                    <p class="card-desc">{app.description}</p>
                  </article>
                ))}
              </div>
            ) : null}

            {section.tip && (
              <div class="sect-tip">
                <span class="tip-ico">{section.tipIcon || '💡'}</span>
                <p>{section.tip}</p>
              </div>
            )}
          </section>
        ))}

        <!-- Good to Know -->
        <section id="good-to-know" class="sect info-sect">
          <div class="sect-head">
            <span class="sect-icon">📋</span>
            <div class="sect-titles"><h2>Good to Know</h2></div>
          </div>

          <div class="info-row">
            <div class="info-card">
              <h3>⬇ Installing Apps</h3>
              <ul>
                <li><strong>Mac App Store</strong> — Sandboxed, auto-updates</li>
                <li><strong>Homebrew Cask</strong> — <code>brew install --cask &lt;app&gt;</code></li>
                <li><strong>Direct download</strong> — From the developer's website</li>
              </ul>
              <div class="brew-box">
                <code>brew install --cask raycast rectangle iterm2 visual-studio-code</code>
              </div>
            </div>
            <div class="info-card">
              <h3>🗑 Uninstalling Apps</h3>
              <p>
                Dragging an app to the Trash does not remove all its files.
                Use <a href="https://freemacsoft.net/appcleaner/">AppCleaner</a> to fully
                remove apps and their preferences, caches, and support&nbsp;files.
              </p>
            </div>
            <div class="info-card">
              <h3>📚 Resources</h3>
              <p>
                <a href="https://formulae.brew.sh/cask/">Homebrew Cask Formulae</a> — Search for
                macOS applications available through Homebrew&nbsp;Cask.
              </p>
            </div>
          </div>
        </section>

        </div>

        <!-- TOC sidebar (component renders both mobile + desktop) -->
        <aside class="apps-sidebar">
          <TableOfContents headings={tocHeadings} />
        </aside>
      </div>

    </div>
  </section>

  <Footer slot="footer" />
  <BackToTop />
</BaseLayout>

<style>
  /* ═══════════════════════════════════════════════
     Recommended Apps — Editorial Catalog
     ═══════════════════════════════════════════════ */

  .apps-page {
    padding: var(--space-xl) 0 var(--space-4xl);
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 var(--space-lg);
  }

  /* ── Hero ─────────────────────────────────────── */
  .hero {
    text-align: center;
    padding: var(--space-2xl) 0 var(--space-xl);
  }

  .hero-ornament {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-md);
    margin-bottom: var(--space-xl);
  }

  .orn-line {
    display: block;
    width: 60px;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--grail-gold), transparent);
  }

  .orn-diamond {
    color: var(--grail-gold);
    font-size: 0.65rem;
    opacity: 0.7;
  }

  .hero h1 {
    font-size: clamp(2.2rem, 5vw, 3.5rem);
    margin-bottom: var(--space-sm);
  }

  .hero-sub {
    font-size: 1.05rem;
    color: var(--turbo-text-secondary);
    margin-bottom: var(--space-xl);
    line-height: 1.6;
  }

  /* ── Controls ────────────────────────────────── */
  .controls {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-md);
    max-width: 480px;
    margin: 0 auto;
  }

  .search-box {
    position: relative;
    width: 100%;
  }

  .search-svg {
    position: absolute;
    left: 14px;
    top: 50%;
    transform: translateY(-50%);
    color: var(--turbo-text-secondary);
    opacity: 0.5;
    pointer-events: none;
  }

  .search-box input {
    width: 100%;
    padding: 10px 16px 10px 40px;
    font-family: var(--font-body);
    font-size: 0.95rem;
    color: var(--turbo-text-primary);
    background: var(--turbo-bg-surface);
    border: 1px solid var(--turbo-border-default);
    border-radius: var(--radius-lg);
    outline: none;
    transition: border-color 0.2s, box-shadow 0.2s;
  }

  .search-box input:focus {
    border-color: var(--grail-gold);
    box-shadow: 0 0 0 3px color-mix(in srgb, var(--grail-gold) 12%, transparent);
  }

  .search-box input::placeholder {
    color: var(--turbo-text-secondary);
    opacity: 0.5;
  }

  .price-filters {
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
    justify-content: center;
  }

  .pf {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    padding: 5px 14px;
    font-family: var(--font-display);
    font-size: 0.72rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--turbo-text-secondary);
    background: none;
    border: 1px solid var(--turbo-border-default);
    border-radius: var(--radius-full);
    cursor: pointer;
    transition: all 0.2s;
  }

  .pf:hover {
    color: var(--turbo-text-primary);
    border-color: color-mix(in srgb, var(--grail-gold) 60%, transparent);
  }

  .pf.active {
    color: var(--grail-gold);
    border-color: var(--grail-gold);
    background: color-mix(in srgb, var(--grail-gold) 10%, transparent);
  }

  .pf-dot {
    display: block;
    width: 6px;
    height: 6px;
    border-radius: 50%;
  }

  .free-dot  { background: #a6e3a1; }
  .freemium-dot { background: #89b4fa; }
  .paid-dot  { background: var(--grail-gold); }

  /* ── Two-column Layout ──────────────────────── */
  .apps-layout {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-2xl);
  }

  @media (min-width: 1200px) {
    .apps-layout {
      grid-template-columns: 1fr 250px;
    }
  }

  .apps-main {
    min-width: 0;
  }

  /* Mobile TOC wrapper: show on mobile, hide on desktop */
  .apps-toc-mobile {
    margin-bottom: var(--space-xl);
  }

  @media (min-width: 1200px) {
    .apps-toc-mobile {
      display: none;
    }
  }

  /* Desktop sidebar: hide on mobile, show on desktop */
  .apps-sidebar {
    display: none;
  }

  @media (min-width: 1200px) {
    .apps-sidebar {
      display: block;
    }
  }

  /* ── Sections ──────────────────────────────────── */
  .sections {
    display: flex;
    flex-direction: column;
    gap: var(--space-3xl);
  }

  .sect {
    scroll-margin-top: calc(70px + var(--space-lg));
    padding-top: var(--space-xl);
  }

  .sect-head {
    display: flex;
    align-items: flex-start;
    gap: var(--space-md);
    margin-bottom: var(--space-lg);
    padding-bottom: var(--space-md);
    border-bottom: 1px solid color-mix(in srgb, var(--grail-gold) 20%, transparent);
  }

  .sect-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 46px;
    height: 46px;
    font-size: 1.4rem;
    background: var(--turbo-bg-surface);
    border: 1px solid var(--turbo-border-default);
    border-radius: 10px;
    flex-shrink: 0;
  }

  .sect-titles h2 {
    margin: 0;
    font-size: 1.4rem;
    line-height: 1.3;
  }

  .sect-titles h2::after { display: none; }

  .sect-note {
    margin: 4px 0 0;
    font-size: 0.88rem;
    color: var(--turbo-text-secondary);
    line-height: 1.4;
  }

  /* ── Groups ────────────────────────────────────── */
  .grp {
    margin-bottom: var(--space-xl);
  }

  .grp-label {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--turbo-text-secondary);
    margin: 0 0 var(--space-sm) 2px;
    opacity: 0.7;
  }

  .grp-label::after { display: none; }

  /* ── Cards Grid ────────────────────────────────── */
  .cards {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: 10px;
  }

  /* ── Card ──────────────────────────────────────── */
  .card {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding: 14px 16px;
    background: var(--turbo-bg-surface);
    border: 1px solid var(--turbo-border-default);
    border-radius: 10px;
    transition: border-color 0.2s, box-shadow 0.2s, transform 0.2s;
  }

  .card:hover {
    border-color: color-mix(in srgb, var(--grail-gold) 45%, transparent);
    box-shadow: 0 2px 16px -2px color-mix(in srgb, var(--grail-gold) 8%, transparent);
    transform: translateY(-1px);
  }

  .card.hidden { display: none; }

  .card-header {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .card-fav-wrap {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    background: var(--turbo-bg-overlay);
    border: 1px solid var(--turbo-border-default);
    border-radius: 7px;
    overflow: hidden;
    flex-shrink: 0;
  }

  .card:hover .card-fav-wrap {
    border-color: color-mix(in srgb, var(--grail-gold) 40%, transparent);
  }

  .card-fav {
    width: 20px;
    height: 20px;
    object-fit: contain;
  }

  .card-fav-fb {
    display: none;
    align-items: center;
    justify-content: center;
    width: 100%;
    height: 100%;
    font-size: 0.85rem;
  }

  .card-meta {
    display: flex;
    align-items: center;
    gap: 8px;
    flex: 1;
    min-width: 0;
  }

  .card-name {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 0.9rem;
    color: var(--turbo-text-primary);
    text-decoration: none;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  a.card-name:hover { color: var(--grail-gold); }
  a.card-name::after { display: none; }

  .ext {
    font-size: 0.7rem;
    opacity: 0;
    transition: opacity 0.15s;
  }

  .card:hover .ext { opacity: 0.4; }

  .card-desc {
    margin: 0;
    font-size: 0.82rem;
    color: var(--turbo-text-secondary);
    line-height: 1.4;
  }

  /* ── Badges ────────────────────────────────────── */
  .badge {
    display: inline-block;
    padding: 1px 9px;
    border-radius: var(--radius-full);
    font-family: var(--font-display);
    font-size: 0.6rem;
    letter-spacing: 0.04em;
    white-space: nowrap;
    flex-shrink: 0;
    line-height: 1.6;
  }

  .badge-type {
    position: absolute;
    top: 10px;
    right: 10px;
  }

  .badge.free {
    background: color-mix(in srgb, #a6e3a1 14%, transparent);
    color: #a6e3a1;
    border: 1px solid color-mix(in srgb, #a6e3a1 28%, transparent);
  }

  .badge.freemium {
    background: color-mix(in srgb, #89b4fa 14%, transparent);
    color: #89b4fa;
    border: 1px solid color-mix(in srgb, #89b4fa 28%, transparent);
  }

  .badge.paid {
    background: color-mix(in srgb, var(--grail-gold) 14%, transparent);
    color: var(--grail-gold);
    border: 1px solid color-mix(in srgb, var(--grail-gold) 28%, transparent);
  }

  .badge.price {
    background: color-mix(in srgb, #cba6f7 14%, transparent);
    color: #cba6f7;
    border: 1px solid color-mix(in srgb, #cba6f7 28%, transparent);
    font-variant-numeric: tabular-nums;
  }

  /* ── Tip ───────────────────────────────────────── */
  .sect-tip {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    margin-top: var(--space-lg);
    padding: 12px 16px;
    background: color-mix(in srgb, var(--grail-gold) 6%, transparent);
    border: 1px solid color-mix(in srgb, var(--grail-gold) 16%, transparent);
    border-left: 3px solid color-mix(in srgb, var(--grail-gold) 50%, transparent);
    border-radius: 8px;
    font-size: 0.85rem;
  }

  .tip-ico { flex-shrink: 0; line-height: 1.5; }

  .sect-tip p {
    margin: 0;
    color: var(--turbo-text-secondary);
    line-height: 1.5;
  }

  /* ── No Results ────────────────────────────────── */
  .no-results {
    text-align: center;
    padding: var(--space-3xl) 0;
  }

  .no-results p {
    font-family: var(--font-display);
    color: var(--turbo-text-secondary);
    font-size: 1rem;
  }

  /* ── Info Cards ────────────────────────────────── */
  .info-row {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-md);
  }

  .info-card {
    padding: var(--space-lg);
    background: var(--turbo-bg-surface);
    border: 1px solid var(--turbo-border-default);
    border-radius: 10px;
    border-top: 2px solid color-mix(in srgb, var(--grail-gold) 35%, transparent);
  }

  .info-card h3 {
    font-size: 0.95rem;
    margin: 0 0 var(--space-sm);
  }

  .info-card h3::after { display: none; }

  .info-card p,
  .info-card li {
    font-size: 0.88rem;
    color: var(--turbo-text-secondary);
    line-height: 1.5;
  }

  .info-card p { margin: 0 0 var(--space-sm); }
  .info-card p:last-child { margin-bottom: 0; }

  .info-card ul {
    margin: 0 0 var(--space-sm);
    padding-left: var(--space-lg);
  }

  .info-card li { margin-bottom: 4px; }

  .brew-box {
    padding: 8px 12px;
    background: var(--turbo-bg-overlay);
    border-radius: 6px;
    overflow-x: auto;
  }

  .brew-box code {
    font-size: 0.78rem;
    background: none;
    border: none;
    padding: 0;
    white-space: nowrap;
  }

  /* ── Responsive ────────────────────────────────── */
  @media (max-width: 480px) {
    .cards {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 600px) {
    .container {
      padding: 0 var(--space-md);
    }

    .hero {
      padding: var(--space-lg) 0 var(--space-md);
    }
  }
</style>

<script>
  function initAppsFilter() {
    const input = document.getElementById('app-search') as HTMLInputElement | null;
    const pills = document.querySelectorAll<HTMLButtonElement>('.pf[data-filter]');
    const cards = document.querySelectorAll<HTMLElement>('.card[data-name]');
    const sects = document.querySelectorAll<HTMLElement>('[data-sect]');
    const grps = document.querySelectorAll<HTMLElement>('[data-grp]');
    const noRes = document.getElementById('no-results');

    let filter = 'all';
    let query = '';

    function run() {
      let total = 0;
      cards.forEach(c => {
        const matchQ = !query || (c.dataset.name || '').includes(query) || (c.dataset.desc || '').includes(query);
        const matchF = filter === 'all' || (c.dataset.price || '') === filter;
        const show = matchQ && matchF;
        c.classList.toggle('hidden', !show);
        if (show) total++;
      });

      sects.forEach(s => {
        if (s.classList.contains('info-sect')) return;
        const vis = s.querySelectorAll('.card:not(.hidden)').length > 0;
        s.style.display = vis ? '' : 'none';
      });

      grps.forEach(g => {
        const vis = g.querySelectorAll('.card:not(.hidden)').length > 0;
        g.style.display = vis ? '' : 'none';
      });

      if (noRes) noRes.style.display = total > 0 ? 'none' : '';
    }

    input?.addEventListener('input', () => { query = input.value.toLowerCase().trim(); run(); });

    pills.forEach(p => p.addEventListener('click', () => {
      pills.forEach(x => x.classList.remove('active'));
      p.classList.add('active');
      filter = p.dataset.filter || 'all';
      run();
    }));
  }

  initAppsFilter();
  document.addEventListener('astro:after-swap', initAppsFilter);
</script>
