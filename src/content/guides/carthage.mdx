---
title: Carthage
description:
  Install Carthage dependency manager for iOS/macOS development and Appium
  WebDriverAgent
category: Testing
order: 19
tags: [ios, mobile, dependencies, testing, carthage]
difficulty: advanced
---

import LinkBadge from '../../components/LinkBadge.astro';
import LinkCard from '../../components/LinkCard.astro';

## What is Carthage?

[Carthage](https://github.com/Carthage/Carthage) is a decentralized dependency
manager for Cocoa projects (iOS, macOS). Unlike CocoaPods, Carthage builds your
dependencies as frameworks and gives you full control over your project structure.

> Carthage may be required for building WebDriverAgent, which Appium uses for iOS
> automation.

## Prerequisites

<div class="badge-group">
  <LinkBadge href="/guides/homebrew/" />
  <LinkBadge href="/guides/xcode/" />
</div>

## Installation

```bash
brew install carthage
```

## Verify Your Installation

```bash
carthage version
```

## Basic Usage

### Create a Cartfile

Create a file named `Cartfile` in your project root:

```ruby
github "Alamofire/Alamofire" ~> 5.0
github "onevcat/Kingfisher" ~> 7.0
```

### Build Dependencies

**For iOS:**

```bash
carthage update --platform iOS
```

**For macOS:**

```bash
carthage update --platform macOS
```

**For all platforms:**

```bash
carthage update
```

### Use Pre-built Binaries

To speed up builds by using pre-built binaries when available:

```bash
carthage update --use-xcframeworks
```

## Cartfile Syntax

### Dependency Sources

```ruby
# GitHub repository
github "owner/repo"

# Git URL
git "https://example.com/repo.git"

# Binary framework
binary "https://example.com/framework.json"
```

### Version Requirements

```ruby
# Exactly version 1.0
github "owner/repo" == 1.0

# At least version 1.0
github "owner/repo" >= 1.0

# Compatible with version 1.0 (semver)
github "owner/repo" ~> 1.0

# Specific branch
github "owner/repo" "develop"

# Specific commit
github "owner/repo" "abc123"
```

## Adding Frameworks to Xcode

### Using XCFrameworks (Recommended)

1. Run `carthage update --use-xcframeworks`
2. In Xcode, select your target
3. Go to **General** > **Frameworks, Libraries, and Embedded Content**
4. Click **+** and select **Add Other** > **Add Files**
5. Navigate to `Carthage/Build/` and select the `.xcframework`

### Using Traditional Frameworks

1. Run `carthage update`
2. Add frameworks from `Carthage/Build/iOS/` or `Carthage/Build/Mac/`
3. Add a Run Script build phase for copying frameworks:

```bash
/usr/local/bin/carthage copy-frameworks
```

Input Files:
```
$(SRCROOT)/Carthage/Build/iOS/SomeFramework.framework
```

## Appium WebDriverAgent

If <LinkBadge href="/guides/appium/" /> requires building WebDriverAgent with Carthage:

1. Navigate to WebDriverAgent directory:
   ```bash
   cd ~/.appium/node_modules/appium-xcuitest-driver/node_modules/appium-webdriveragent
   ```

   > **Note:** The exact path to `appium-webdriveragent` may vary depending on your
   > installation method (global vs. local, npm vs. other package managers). To locate
   > the correct directory, search for `appium-webdriveragent` in your global or
   > project-local `node_modules`, or run `npm ls -g appium-xcuitest-driver` to confirm
   > the install location.

2. Bootstrap dependencies:
   ```bash
   ./Scripts/bootstrap.sh
   ```

This script uses Carthage to fetch and build WebDriverAgent dependencies.


## Troubleshooting

### "Build Failed"

Clear the cache and retry:

```bash
rm -rf ~/Library/Caches/org.carthage.CarthageKit
carthage update --platform iOS
```

### Xcode Version Mismatch

If using multiple Xcode versions:

```bash
sudo xcode-select -s /Applications/Xcode.app
carthage update --platform iOS
```

### Module Not Found

Ensure the framework is properly linked in Xcode:
1. Check **Build Settings** > **Framework Search Paths**
2. Add `$(PROJECT_DIR)/Carthage/Build/iOS` if missing

### Update Carthage

```bash
brew upgrade carthage
```

## Resources

<LinkCard
  href="https://github.com/Carthage/Carthage"
  title="Carthage on GitHub"
  description="Official repository with documentation and releases"
/>
<LinkCard
  href="https://www.raywenderlich.com/7936-carthage-tutorial-getting-started"
  title="Carthage Tutorial"
  description="Getting started guide from Ray Wenderlich"
/>
