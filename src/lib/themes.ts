/**
 * Theme defaults for Holy Grail documentation
 *
 * The turbo-themes selector manages the full theme registry.
 * These defaults are only used for FOUC prevention.
 */

export const DEFAULT_DARK_THEME = "catppuccin-mocha";
export const DEFAULT_LIGHT_THEME = "catppuccin-latte";
