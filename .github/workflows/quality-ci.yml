---
name: Quality CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  merge_group:

concurrency:
  group: quality-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-quality-lint.yml@86e29ba560701a8d73f31713eb0841ef50f6029c # v0.74.5
    with:
      job-name: '🛠️ Lintro Code Quality'
      tooling-ref: '86e29ba560701a8d73f31713eb0841ef50f6029c' # v0.74.5
    permissions:
      contents: read
      packages: read

  publish-quality-summary:
    needs: quality
    if: >-
      !cancelled()
      && github.event_name == 'pull_request'
      && github.event.pull_request.head.repo.fork == false
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-publish-quality-summary.yml@86e29ba560701a8d73f31713eb0841ef50f6029c # v0.74.5
    with:
      exit-code: ${{ needs.quality.outputs.exit-code }}
      tooling-ref: '86e29ba560701a8d73f31713eb0841ef50f6029c' # v0.74.5
    permissions:
      contents: read
      pull-requests: write

  build:
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-build-artifact.yml@86e29ba560701a8d73f31713eb0841ef50f6029c # v0.74.5
    with:
      job-name: '🏗️ Build & Test'
      build-command: bash scripts/ci/build-site.sh
      node-version: '22.12.0'
      artifact-name: site-dist
      artifact-path: dist
      tooling-ref: '86e29ba560701a8d73f31713eb0841ef50f6029c' # v0.74.5
    permissions:
      contents: read
